## API-config steps — دليل إعداد واجهة برمجة التطبيقات (Dolibarr API) وربط البوابة

هذا الدليل يوضح باختصار كيفية تفعيل صلاحيات واجهة برمجة التطبيقات في Dolibarr، إنشاء مفتاح API صالح، واختباره، ثم ربطه ببوابة العميل (Next.js) بحيث تعمل نقاط النهاية مثل /api/invoices بدون أخطاء 403.

### المتطلبات
- Dolibarr v22 أو أحدث مع REST API مفعّلة (المسار: https://<ERP_HOST>/api/index.php).
- وصول إداري لإدارة المستخدمين والصلاحيات.
- الوصول للخادم الذي يشغّل البوابة لتعديل ملف البيئة وإعادة تشغيل الخدمة.

---

## 1) إعداد Dolibarr

1. تفعيل الوحدات المطلوبة
   - الإعدادات → الوحدات/التطبيقات:
     - Web services / API (REST) → مفعّل.
     - Third Parties (الطرف الثالث)، Billing/Invoices (الفوترة/الفواتير)، وأي وحدة ستحتاجها (مثل Tickets) → مفعّلة.

2. إنشاء/اختيار حساب خدمة للمفاتيح
   - المستخدمون والمجموعات → المستخدمون → اختر مستخدم بوابة مخصص (يفضّل حساب خدمة).
   - تأكد أن المستخدم «نشِط».
   - إن كان مستخدمًا خارجيًا (external)، اربطه بالطرف الثالث الصحيح من بطاقة المستخدم.

3. توليد مفتاح API
   - من بطاقة المستخدم، ابحث عن قسم «Web services / API key» ثم أنشئ مفتاحًا جديدًا.
   - انسخ المفتاح وحافظ عليه بسرية.

4. منح الأذونات الدنيا اللازمة
   - من صلاحيات المستخدم (أو المجموعة التي ينتمي لها):
     - Third parties: Read
     - Billing/Invoices: Read customer invoices (وأي صلاحيات قراءة مدفوعات عند الحاجة)
     - Tickets/Support: إنشاء/قراءة إن كنت ستستخدم التذاكر
   - للمستخدم الخارجي: فعّل صلاحيات «قراءة شركته» و«قراءة فواتيره» وما يلزم.

5. اختبار مباشر للمفتاح (يجب أن يعيد 200)
   - مثال اختبار عبر الهيدر DOLAPIKEY:
     - GET https://erp.digitalpin.online/api/index.php/invoices?limit=1
     - Header: DOLAPIKEY: <API_KEY>
   - ملاحظات الحالة:
     - 200: نجاح وصلاحيات كافية.
     - 403: المفتاح معروف لكن الصلاحيات غير كافية (حدّث أذونات المستخدم).
     - 401: لم يتم تمرير المفتاح بالشكل الصحيح (استخدم هيدر DOLAPIKEY أو بارامتر DOLAPIKEY في الرابط).

6. ملاحظات إضافية
   - 403 على /users للمستخدمين غير الإداريين أمر متوقع؛ اختبر على /invoices و/thirdparties.
   - إن كان /api/index.php/explorer يعطي 404، فعّل دعم PATH_INFO في خادم الويب.

---

## 2) ربط المفتاح بالبوابة (customer-portal)

1. إعداد ملف البيئة للإنتاج
   - حرّر الملف customer-portal/.env.production.local وضع القيم:
     - DOL_API_BASE=https://erp.digitalpin.online/api/index.php
     - DOL_API_KEY=<API_KEY>
     - NEXTAUTH_URL=https://app.digitalpin.online
     - NEXTAUTH_SECRET=<سِر عشوائي قوي> (يفضّل أحرف خاصة؛ إن استخدمت EnvironmentFile لـ systemd، ضعه بين علامتي اقتباس)

2. إعادة تشغيل خدمة البوابة
   - تعمل البوابة بوضع Next.js standalone وتحمّل .env.production.local في وقت التشغيل عبر dotenv، لذا يكفي إعادة التشغيل.
   - بعد الإعادة، اختبر:
     - https://app.digitalpin.online/api/invoices → يجب أن يعيد JSON بدل 403.

3. أمان جيد مُستحسن
   - أقل صلاحيات ممكنة لحساب الخدمة.
   - تدوير دوري لمفتاح API عند الحاجة.
   - لا تضع المفاتيح في Git؛ استخدم ملفات .env المحلية و/أو مخزن أسرار.

---

## 3) استكشاف الأخطاء وإصلاحها

- 403 من ERP مباشرة: المفتاح صحيح لكن الصلاحيات ناقصة → عدّل أذونات المستخدم/المجموعة.
- 401 من ERP: لم يُقرأ المفتاح → تأكد من استخدام اسم الهيدر الصحيح DOLAPIKEY أو بارامتر DOLAPIKEY.
- 404 على explorer: راجع إعداد PATH_INFO في Nginx/PHP.
- ما زالت البوابة تعيد 500: تأكد من وجود DOL_API_BASE وDOL_API_KEY في .env.production.local وإعادة تشغيل الخدمة.

---

## 4) أمثلة أوامر اختبار (اختياري)

استبدل <API_KEY> بالقيمة الفعلية:

```
# اختبار عبر الهيدر (مفضل)
curl -H "DOLAPIKEY: <API_KEY>" \
  "https://erp.digitalpin.online/api/index.php/invoices?limit=1"

# بديل عبر بارامتر الاستعلام
curl "https://erp.digitalpin.online/api/index.php/invoices?limit=1&DOLAPIKEY=<API_KEY>"
```

إذا عاد 403، راجع قسم الأذونات في الخطوة (1-4).
